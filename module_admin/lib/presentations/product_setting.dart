import 'package:flutter/widgets.dart';

class ProductSetting extends StatelessWidget {
  const ProductSetting({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Product Setting');
  }
}